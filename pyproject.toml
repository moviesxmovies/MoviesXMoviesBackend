[project]
name = "moviesxmoviesbackend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.1",
    "pillow>=12.1.0",
    "prettyconf>=2.3.0",
    "pytest-cov>=7.0.0",
    "pytest-django>=4.11.1",
]

[dependency-groups]
dev = [
    "pytest-django>=4.11.1",
    "pytest-rich>=0.2.0",
]
